---
description: Turn AFK auto-resume on or off, or check which delivery channel is live
allowed-tools: Bash(afk:*)
---

Run `afk $ARGUMENTS` (default `doctor` when no argument is given) and report the
result to the user in one or two lines. Do not change any other AFK state.
